Eres el editor del repo RideMe (monorepo turbo, app principal en apps/web, Next.js 14 App Router, Clerk, Neon, Resend, Google Maps, framer-motion). RideMe es ride-hailing "name your price". NO cambies su proposito. Trabaja SOLO en /root/RideMe. Lleva la app a 100% premium. Haz TODOS estos cambios y deja `npm run build` (en apps/web) en VERDE:

1) ELIMINAR lucide-react por completo. Crea apps/web/src/components/icons.tsx exportando componentes SVG inline (props: size?:number, className?:string, color?:string) para TODOS los nombres usados en el repo (Car, DollarSign, Shield, Zap, Star, ChevronRight, MapPin, Clock, ArrowRight, Menu, X, User, Settings, Bell, Search, Check, Plus, Edit, Trash, Home, Map, History, Wallet, LogOut, Sun, Moon, Phone, Mail, Send, MessageCircle, AlertCircle, Upload, Filter, ChevronDown, ChevronLeft, Eye, etc. — escanea con grep todos los imports de lucide-react y cubre cada nombre). Reemplaza cada `import { X } from 'lucide-react'` por `import { X } from '@/components/icons'`. Quita lucide-react de package.json. Iconos limpios, trazo 2, currentColor por defecto.

2) MODO DEMO sin auth: edita src/middleware.ts para que si la cookie `rideme_demo=1` esta presente, NO se llame auth().protect() (permite navegar /app, /admin, /driver sin login). Crea ruta src/app/demo/route.ts que set-cookee rideme_demo=1 y redirija a ?to= (default /app). En la landing agrega botones "Ver demo" (usuario→/demo?to=/app), "Demo conductor" (/demo?to=/driver) y "Demo admin" (/demo?to=/admin). Mantener Clerk para login real.

3) SOPORTE in-app: componente src/components/SupportButton.tsx (boton flotante "Reportar" abajo-derecha, abre modal con textarea + opcional screenshot URL). POST a src/app/api/support/route.ts que inserta en tabla Neon support_tickets(project,user_ref,message,screenshot,ts,status) usando process.env.SUPPORT_DATABASE_URL (neon http). project='rideme'. Manda email al admin via Resend (RESEND_API_KEY) si existe. Incluir <SupportButton/> en layout de (user), (driver), admin y landing. Crea src/app/admin/support/page.tsx que hace GET /api/support (lista tickets rideme, polling cada 5s) y permite PATCH status a 'resolved'. Agrega link "Soporte" en el nav admin.

4) WHITE-LABEL admin: src/app/admin/branding/page.tsx con form (nombre app, color primario, color acento, logo URL, icono URL, modo dia/noche default). Guarda en tabla Neon app_branding(project PK,'rideme', app_name,primary_color,accent_color,logo_url,icon_url,theme,updated_at) via /api/branding (GET/PUT, SUPPORT_DATABASE_URL). Crea src/components/BrandingProvider.tsx (client) que hace fetch /api/branding, aplica CSS vars (--brand-primary,--brand-accent), document.title y favicon dinamico. Envuelve la app en providers.tsx. Link "Personalizacion" en nav admin.

5) COMMS usuario<->admin: en /admin/support, al resolver o responder un ticket, enviar email al user_ref via Resend. Agrega campo de respuesta. Polling = tiempo real suficiente.

6) PWA: verifica apps/web/public/manifest.json (name RideMe, short_name, start_url '/', display 'standalone', theme_color, background_color, icons 192 y 512 maskable apuntando a /brand/icon-192.png y /brand/icon-512.png). Asegura que next.config.js registra next-pwa (runtimeCaching basico) y que layout.tsx incluye <link rel=manifest>, theme-color y apple-touch-icon. Si faltan iconos, crea placeholders PNG simples en public/brand/ (icon-192.png, icon-512.png, apple-touch-icon.png) — puedes generarlos con un script node/sharp o copiar de /home si hay.

7) RESPONSIVE v3 + dia/noche: revisa landing, (user)/app, (driver), admin. Scroll vertical real en movil y escritorio (sin position:fixed que tape contenido; usar min-h-screen y overflow correcto). Movil: bottom-nav; escritorio: sidebar/grids a viewport completo. Sin overlays que bloqueen. Toggle dia/noche persistente (cookie/localStorage rideme_theme) con buen contraste en ambos.

8) Imagenes: la landing y dashboards deben usar /brand/hero.jpg, /brand/driver.jpg, /brand/app.jpg (se generan aparte). Usa next/image o <img> con fallback; CERO urls de cloudfront o externas rotas. Si una imagen no existe aun, referencia /brand/hero.jpg igualmente (se agregara).

Reglas: TypeScript estricto, no rompas rutas existentes, usa los componentes/estilos ya presentes (tailwind). Al terminar corre `cd /root/RideMe/apps/web && npm install --no-audit --no-fund && npm run build` y arregla TODOS los errores hasta build verde. Haz commits atomicos. Resume al final que cambiaste.

